# Snapmart — Frontend

This folder contains the frontend scaffold for the Snapmart project (Vite + React + Tailwind).

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Key files:
- `src/main.jsx` — app entry
- `src/routes/AppRoutes.jsx` — routing
- `src/layouts/MainLayout.jsx` — page layout
